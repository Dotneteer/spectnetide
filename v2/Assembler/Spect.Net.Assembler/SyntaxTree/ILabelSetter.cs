namespace Spect.Net.Assembler.SyntaxTree
{
    /// <summary>
    /// This interface signs that an object sets a label value on its own.
    /// </summary>
    public interface ILabelSetter
    {
    }
}