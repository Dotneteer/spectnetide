<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:userControls="clr-namespace:Spect.Net.Z80Tests.UserControls"
             xmlns:debug="clr-namespace:Spect.Net.Z80Tests.ViewModels.Debug" x:Class="Spect.Net.Z80Tests.Views.SpectrumDebugView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="1200" d:DataContext="{d:DesignInstance {x:Type debug:SpectrumDebugViewModel}, IsDesignTimeCreatable=True}" KeyDown="OnKeyDown" KeyUp="OnKeyUp" Focusable="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <userControls:SpectrumDisplayControl x:Name="SpectrumControl"  HorizontalAlignment="Left" VerticalAlignment="Top" Focusable="False" />
        <userControls:DisassemblyListControl Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" DisassemblySource="{Binding Disassembly.DisassemblyItems}"/>
    </Grid>
</UserControl>
